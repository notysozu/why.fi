---
wave: 3
depends_on: [03-02]
files_modified:
  - frontend/src/components/WebcamCapture.tsx
  - frontend/src/App.css
autonomous: true
requirements: [UI-04]
---

# Plan 03-03: Build Hostile UX

<objective>
Implement the hostile UX mechanisms that trap the user in the experience.
</objective>

<action_plan>
<task>
<read_first>
- frontend/src/components/WebcamCapture.tsx
</read_first>
<action>
In `frontend/src/App.css`:
Add styles for `.hostile-btn` (absolute positioning, high z-index).
In `frontend/src/components/WebcamCapture.tsx`:
Add a "Quit Simulation" button.
If `gameScore` < 5:
  - Attach an `onMouseEnter` handler to the button.
  - When triggered, mathematically calculate a new random absolute (top, left) position within the container bounds, and set it via inline `style`.
  - The button should jump away before it can be clicked.
If `gameScore` >= 5:
  - The button statically remains in place and clicking it can stop the webcam stream (alert "You escaped").
Optionally, if `whyScore` > 50, update a `snarkyMessage` state to randomly taunt the user.
</action>
<acceptance_criteria>
- "Quit" button renders.
- `onMouseEnter` trigger translates the button's position.
- Logic is conditional on `gameScore < 5`.
</acceptance_criteria>
</task>
</action_plan>

<verification>
- Verify that `onMouseEnter` causes re-rendering of position strings in `WebcamCapture.tsx`.
</verification>

<must_haves>
- The exit button mathematically evades the cursor.
- The user is trapped until passing the target score.
</must_haves>
