---
wave: 1
depends_on: []
files_modified:
  - frontend/src/components/WebcamCapture.tsx
  - frontend/src/App.css
autonomous: true
requirements: [UI-01, UI-02]
---

# Plan 03-01: Build UI Overlay & Why Meter

<objective>
Create the frontend overlay to display the "Why Meter" tracking real-time data from the WebSocket.
</objective>

<action_plan>
<task>
<read_first>
- frontend/src/components/WebcamCapture.tsx
- frontend/src/App.css
</read_first>
<action>
In `frontend/src/App.css`, add styles for:
- `.overlay-container`: A position-relative wrapper for the webcam.
- `.why-meter-container`: A bar overlaid on the video (absolute, bottom).
- `.why-meter-fill`: The active fill of the progress bar (transition: width 0.1s).
- `.score-text`: Text overlay for why_score.

In `frontend/src/components/WebcamCapture.tsx`:
Add a `whyScore` state variable initialized to 0.
Update the `wsRef.current.onmessage` handler to parse `event.data` (which is JSON).
Extract `why_score` from `data.why_meter.why_score` and set the state.
Change the render method to wrap the `<video>` tag in a `.overlay-container` div, and add the Why Meter visual UI below or overlaid on it.
</action>
<acceptance_criteria>
- `App.css` contains overlay styles.
- `WebcamCapture.tsx` parses JSON from the WebSocket.
- `WebcamCapture.tsx` correctly renders the Why Meter UI mapped to state.
</acceptance_criteria>
</task>
</action_plan>

<verification>
- Verify `App.css` exists with new styles.
- Code review `WebcamCapture.tsx` to confirm JSON parsing in `onmessage`.
</verification>

<must_haves>
- The Why Meter renders on top of the webcam feed.
- It dynamically updates based on WebSocket data.
</must_haves>
