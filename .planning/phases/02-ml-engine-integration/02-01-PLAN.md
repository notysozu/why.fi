---
wave: 1
depends_on: []
files_modified:
  - backend/requirements.txt
  - backend/ml_engine.py
  - backend/main.py
autonomous: true
requirements: [ML-01]
---

# Plan 02-01: Integrate MediaPipe for landmark extraction in FastAPI

<objective>
Install MediaPipe and create a module to instantiate `FaceMesh` and parse incoming frames into 3D landmarks.
</objective>

<action_plan>
<task>
<read_first>
- backend/requirements.txt
- .planning/ROADMAP.md
</read_first>
<action>
Update `backend/requirements.txt` to include `mediapipe`, `opencv-python-headless`, and `numpy`.
Create a new file `backend/ml_engine.py`.
Import `mediapipe as mp` and `numpy as np`, `cv2`.
Create a singleton or reusable class `FaceAnalyzer` that initializes `mp.solutions.face_mesh.FaceMesh(static_image_mode=False, max_num_faces=1, refine_landmarks=True)`.
Add a method `process_frame(base64_string)` that decodes the base64 jpeg into a numpy cv2 image, passes it to the `FaceMesh.process()`, and returns the extracted `face_landmarks`.
</action>
<acceptance_criteria>
- `backend/requirements.txt` contains `mediapipe`.
- `backend/ml_engine.py` contains `FaceMesh` initialization.
- `backend/ml_engine.py` contains `process_frame`.
</acceptance_criteria>
</task>
</action_plan>

<verification>
- `cat backend/requirements.txt | grep mediapipe`
- Verify `backend/ml_engine.py` parses base64 and returns landmarks via automated check or code review.
</verification>

<must_haves>
- Server initializes MediaPipe without crashing.
- Method exists to convert base64 image strings to MediaPipe objects.
</must_haves>
