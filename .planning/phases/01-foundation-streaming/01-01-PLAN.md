---
wave: 1
depends_on: []
files_modified:
  - backend/main.py
  - backend/requirements.txt
  - frontend/package.json
  - frontend/vite.config.ts
autonomous: true
requirements: []
---

# Plan 01-01: Create baseline React app and FastAPI server structure

<objective>
Initialize the React (Vite) frontend and FastAPI backend project structures.
</objective>

<action_plan>
<task>
<read_first>
- .planning/ROADMAP.md
</read_first>
<action>
Create a new Vite React TypeScript project in the `frontend` directory using `npm create vite@latest frontend -- --template react-ts`.
Install standard dependencies if needed.
</action>
<acceptance_criteria>
- `frontend/package.json` exists and contains "react" and "vite".
</acceptance_criteria>
</task>

<task>
<read_first>
- .planning/ROADMAP.md
</read_first>
<action>
Create a new Python environment and FastAPI project in the `backend` directory.
Create `backend/requirements.txt` with `fastapi`, `uvicorn`, `websockets`.
Create `backend/main.py` with a basic FastAPI app and a `/health` endpoint returning `{"status": "ok"}`.
Include CORS middleware allowing `http://localhost:5173` and `http://127.0.0.1:5173`.
</action>
<acceptance_criteria>
- `backend/main.py` contains `def health()`.
- `backend/requirements.txt` contains `fastapi`.
</acceptance_criteria>
</task>
</action_plan>

<verification>
- `cat backend/main.py | grep health` returns the health endpoint.
- `cat frontend/package.json | grep react` returns the dependency.
</verification>

<must_haves>
- FastAPI runs on port 8000 with CORS configured for localhost:5173.
- React app is initialized and runnable.
</must_haves>
